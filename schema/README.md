# Reserved for manifest schema files.

Planned schema additions should include manifest validation for the implemented build-extension fields described in `docs/HOOKS.md`, plus later expansions such as defines and link settings.
